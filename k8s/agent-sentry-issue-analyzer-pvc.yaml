apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: agent-sentry-issue-analyzer
  namespace: '{{ "NAMESPACE" | env }}'
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
