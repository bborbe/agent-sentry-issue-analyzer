apiVersion: v1
kind: ResourceQuota
metadata:
  name: agent-claude
  namespace: prod
spec:
  hard:
    pods: "1"
  scopeSelector:
    matchExpressions:
      - scopeName: PriorityClass
        operator: In
        values: ["agent-claude"]
