apiVersion: v1
kind: ResourceQuota
metadata:
  name: agent-sentry-issue-analyzer
  namespace: dev
spec:
  hard:
    pods: "1"
  scopeSelector:
    matchExpressions:
      - scopeName: PriorityClass
        operator: In
        values: ["agent-sentry-issue-analyzer"]
